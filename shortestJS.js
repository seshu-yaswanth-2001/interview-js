// empty file with .js is the shortest js program
/**
 * what happens when we run an empty file javascript creates three things in any time
 *  
 * 1) Global Object (window), 2) Global Execution Context (GEC), 3) this.
 *
 * as we already know GEC
 *
 * let talk about window - has the builtin functions if any gloabl variables or functions
 * are created it will store here
 * 
 * this in global level is window in browser, this may be changable based on the environmet 
 * js is running for browser this is nothing but window
 * 
 * this === window is true
 * 
 * var x = 10;
 * console.log(window.x); // 10
 * console.log(x) // is same as above at global if nothing specified it is referring to window object
 * 
 */
